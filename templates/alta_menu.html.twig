{% extends "base.html.twig" %}

{% block title %}Alta de Compra{% endblock %}

{% block container %}
    {% include 'aside.html.twig' %}
    <section id="ancho_parcial">
        <article>
            <h1>Registrar Menu del dia</h1>
            <form method="POST" id="alta_menu" name="alta_menu" action="./?action=alta_menu_system">
                <input type="hidden" value="{{token}}" name="token">
                <h3>Fecha:</h3>
                <input style="width: 40%;" {% if fecha == null %}value="{{ "now"|date("Y-m-d", "America/Argentina/Buenos_Aires")  }}" {% else %} value="{{ fecha | date("Y-m-d") }}" {% endif %} type="date" class="classText" id="fecha" name="fecha" min="{{ "now"|date("Y-m-d", "America/Argentina/Buenos_Aires")  }}" max="2100-01-01" required>
                <h3>Productos:</h3>
                <button type="button" id="sumarProd" class="buttonDatalistCompra">Agregar Producto</button>
                <div id="listaProductos">
                    <div id="1">
                        <input id="productoCompra" class="datalistCompra" list="productos" name="producto1" placeholder="Buscar producto por nombre" maxlength="100" required>
                        <datalist id="productos">
                            {% for p in products %}
                            <option value="{{ p.nombre }}">
                                {% endfor %}
                        </datalist>
                    </div>
                </div>
                <input id="cantProductos" name="cantProductos" type="hidden" value="1">
                <input type="submit" value="Dar de alta" id="confirmarCompra">
                <input type="reset" value="Restablecer">
            </form>
        </article>
    </section>
{% endblock %}